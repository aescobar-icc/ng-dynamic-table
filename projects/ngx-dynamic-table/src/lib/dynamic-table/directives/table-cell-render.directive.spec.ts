import { TableCellRenderDirective } from './table-cell-render.directive';

describe('TableCellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
